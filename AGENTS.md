1. O3Pilot 可以读取 Ozon 数据、分析 Ozon 数据、接收 Ozon Webhook，但永远不主动修改 Ozon 上的商品、价格、库存、订单、推广或配置
2. 目前处于开发阶段，只将Google Drive中的“O3Pilot”作为项目仓库，每轮对话前都审查一遍文件夹中所有内容和代码，但不要在未经我允许的情况下进行修改
3. 项目原仓库为https://github.com/LI5ee3/O3Pilot-Backup，你可以查看之前的内容作为实现经验参考，但不应该作为新项目的实现约束
4. 每次返回结果后，要完整逐段审查所有修改的内容/代码是否符合预定设想，是否有BUG
5. Google Drive中的“开发参考资料”仅作为开发参考资料，不是 O3Pilot 的运行时数据源，用于设计和验证 O3Pilot，但不是产品运行时依赖。每轮对话前都读一遍“开发参考资料”中的所有内容。